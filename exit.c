#include "shell.h"

/**
 * _exit - Terminates the shell program with a status code of 0.
 *
 * @args: An array of strings containing the command and its arguments.
*/

void _exit(char **args)
{
    exit(0);
}